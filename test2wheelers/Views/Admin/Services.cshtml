@model List<_2whealers.Models.Services>
@{
    ViewBag.Title = "Services";
    Layout = "~/Views/Shared/_Admin_Layout.cshtml";
}



<ol class="breadcrumb mb-0">
    <li class="breadcrumb-item">
        <a href="/"><i class="fas fa-home"></i> Home</a>
    </li>
    <li class="breadcrumb-item active"> Services </li>
</ol>

<div class="row gx-4">
    <div class="col-12">
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h2 class="mb-4"><i class="fas fa-headset"></i> Services</h2>

                <div class="mb-3 text-right">
                    <a href="@Url.Action("AddServices", "Admin")" class="btn btn-success">
                        + Add Services
                    </a>
                </div>

                <div class="table-responsive">
                    <table id="ServicesTable" class="table table-striped table-hover table-bordered align-middle shadow-sm rounded overflow-hidden">
                        <thead style="background: linear-gradient(90deg, #6c63ff, #4facfe); color: #fff;">
                            <tr>
                                <th style="width:60px;" class="text-center">SNo.</th>
                                <th>Owner Details</th>
                                <th>Model Name</th>
                                <th>Date of Service</th> 
                                <th>Amount</th>
                                <th>Date Of Service Reminder</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                int rowNumber = 1;
                            }
                            @if (Model != null && Model.Any())
                            {
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="text-center">@rowNumber</td>
                                        <td class="text-center">
                                            @item.Name <br />
                                            <small>@item.MobileNo</small>
                                        </td>
                                        <td class="text-center">@item.ModelName</td>
                                        <td class="text-center">@Convert.ToDateTime(@item.dateOfSale).ToString("dd-MM-yyyy")</td>
                                        <td class="text-center">@item.Amount</td>
                                        <td class="text-center">@item.DateOfServiceReminder?.ToString("dd-MM-yyyy")</td>
                                        <td class="text-center">
                                            <a href="@Url.Action("ServicesDetails", "Admin", new { id = item.Id })" class="btn btn-sm btn-primary">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                        </td>
                                    </tr>
                                    rowNumber++;
                                }
                            }
                        </tbody>
                    </table>
                </div>

                <style>
                    /* Gradient button */
                    .btn-gradient {
                        background: linear-gradient(45deg, #6c63ff, #4facfe);
                        color: #fff;
                        border: none;
                    }

                        .btn-gradient:hover {
                            background: linear-gradient(45deg, #4facfe, #6c63ff);
                            color: #fff;
                        }

                    /* Table striping */
                    .table-striped tbody tr:nth-of-type(odd) {
                        background-color: #f9f9ff;
                    }

                    /* Hover effect */
                    .table-hover tbody tr:hover {
                        background-color: #eef2ff;
                        transition: 0.3s;
                    }
                </style>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(function () {
            toastr.options = {
                "positionClass": "toast-bottom-right"
            }
        @if (TempData["Success"] != null)
        {
            <text>toastr.success("@TempData["Success"]");</text>
        }
        @if (TempData["Error"] != null)
        {
            <text>toastr.error("@TempData["Error"]");</text>
        }
        });
    </script>
}
<script>
    $(document).ready(function () {
        new DataTable('#ServicesTable', {
            language: {
                emptyTable: "No Services found"
            },
            layout: {
                topStart: {
                    buttons: ['copyHtml5', 'excelHtml5', 'csvHtml5', 'pdfHtml5']
                }
            }
        });
    });
</script>
